language: vim

before_script: |
  git clone https://github.com/junegunn/vader.vim.git
  git clone https://github.com/junegunn/vim-ruby-x.git

script: |
  vim -Nu <(cat << VIMRC
  set rtp+=vader.vim
  set rtp+=vim-ruby-x
  VIMRC) -c 'Vader! test/*'

